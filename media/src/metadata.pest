WHITESPACE = _{ " "+ | "\t"+ }

input = { SOI ~ line* ~ EOI }

line = { source ~ (!":" ~ ASCII)* ~ ":" ~ key ~ value ~ NEWLINE? }

source   = @{ (ASCII_ALPHANUMERIC | "_")+ }
key      = @{ (ASCII_ALPHANUMERIC | "_")+ }

value = @{ (!NEWLINE ~ ANY)* }

NEWLINE = _{ "\n" | "\r\n" }
